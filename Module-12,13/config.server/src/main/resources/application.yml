spring.application.name: config-server
spring:
  cloud:
    config:
      server:
        git:
          #github url then go github make file properties an write global properties file for microservices
          uri: https://github.com/Anuj-Kumar-Sharma/ecommerce-config-server
          username: username
          password: token
          default-label: master
server:
  port: 8888
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka